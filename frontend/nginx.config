server {
    listen 80;
    index index.html;
    server_name localhost;
    error_log  /var/log/nginx/error.log;

    root /usr/share/nginx/html;
    
    location /api {
        proxy_pass http://back/api;
    }
    location /management {
        proxy_pass http://back/management;
    }
    location /swagger-resources {
        proxy_pass http://back/swagger-resources;
    }
    location /v2 {
       proxy_pass http://back/v2;
    }
    location /auth {
       proxy_pass http://back/auth;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
